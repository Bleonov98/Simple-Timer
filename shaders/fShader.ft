#version 400 core

in vec2 TexCoords;
out vec4 FragColour;

uniform sampler2D image;
uniform vec3 objectColour;

void main()
{
    vec4 texColour = texture(image, TexCoords);
    FragColour = vec4(objectColour, texColour.a);
}